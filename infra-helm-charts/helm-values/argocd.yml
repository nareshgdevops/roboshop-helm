server:
  ingress:
    enabled: true
    ingressClassName: "nginx"
    hostname: "argocd-dev.nareshdevops1218.online"
    tls: true
configs:
  certificate:
    enabled: true
    issuer:
      group: "cert-manager.io"
      kind: "ClusterIssuer"
      name: "letsencrypt"
